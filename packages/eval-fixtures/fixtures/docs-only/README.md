# widget-docs

Documentation for the Widget API.

## Getting started

```
npm install widget
```

Then import it:

```ts
import { Widget } from 'widget';
const w = new Widget({ size: 'medium' });
```

See `docs/api.md` for the full reference.
