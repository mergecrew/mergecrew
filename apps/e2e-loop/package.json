{
  "name": "@mergecrew/e2e-loop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "e2e": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run"
  },
  "dependencies": {
    "@mergecrew/sandbox-driver": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
