{
  "name": "@mergecrew/runner-dns",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && vitest run"
  },
  "dependencies": {
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "pino-pretty": "^11.2.2",
    "tsx": "^4.22.3",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
