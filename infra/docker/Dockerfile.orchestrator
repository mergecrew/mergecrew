ARG NODE_VERSION=22-bookworm-slim

FROM node:${NODE_VERSION} AS base
RUN corepack enable
WORKDIR /repo

FROM base AS deps
COPY pnpm-workspace.yaml package.json pnpm-lock.yaml* tsconfig.base.json ./
COPY apps/orchestrator/package.json apps/orchestrator/
COPY packages/ packages/
RUN pnpm install --frozen-lockfile=false

FROM deps AS build
COPY . .
RUN pnpm --filter @mergecrew/db generate
RUN pnpm --filter @mergecrew/orchestrator... build

FROM node:${NODE_VERSION} AS runtime
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends openssl ca-certificates \
  && rm -rf /var/lib/apt/lists/*
COPY --from=build /repo/node_modules /app/node_modules
COPY --from=build /repo/apps/orchestrator/node_modules /app/apps/orchestrator/node_modules
COPY --from=build /repo/apps/orchestrator/dist /app/apps/orchestrator/dist
COPY --from=build /repo/apps/orchestrator/package.json /app/apps/orchestrator/package.json
COPY --from=build /repo/packages /app/packages
RUN chown -R node:node /app
USER node
WORKDIR /app/apps/orchestrator
CMD ["node", "dist/main.js"]
